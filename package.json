{
  "name": "hub-pbm-doc",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.104"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist",
    "buildjson": "redocly bundle --ext json -o dist.json",
    "test": "redocly lint"
  }
}
